#machine:
#  services:
#    - docker

dependencies:
  override:
    - pip install
    - pip install docker-compose
    - pip install --upgrade six # fix error with docker-compose
    - cd docker && docker-compose up -d
    - sleep 30

test:
  override:
    - docker-compose exec -T tests /data/vendor/phpunit/phpunit/phpunit -c /data/phpunit.xml

