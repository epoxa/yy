machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - cd docker
    - pwd
    - ls -l
    - docker-compose up -d
    - sleep 30

test:
  override:
    - docker-compose exec -T tests /data/vendor/phpunit/phpunit/phpunit -c /data/phpunit.xml

